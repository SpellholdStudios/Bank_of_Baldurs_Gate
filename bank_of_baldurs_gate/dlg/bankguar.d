BEGIN BANKGUAR





IF ~

RandomNum(10,1)
~ THEN BEGIN FM0 // from:
  SAY ~Don't try to steal anything. I am watching you.~
  IF ~~ THEN EXIT
END

IF ~

RandomNum(10,2)~ THEN BEGIN FM1 // from:
  SAY ~Talk to superiors, I am on guard there.~
  IF ~~ THEN EXIT
END

IF ~

RandomNum(10,3)
~ THEN BEGIN FM2 // from:
  SAY ~I am resposible for order there, don't try something stoopid.~
  IF ~~ THEN EXIT
END

IF ~

RandomNum(10,4)
~ THEN BEGIN FM3 // from:
  SAY ~Don't bother me, I am on work here.~
  IF ~~ THEN EXIT
END

IF ~

RandomNum(10,5)
~ THEN BEGIN FM4 // from:
  SAY ~Say, isn't my new armor is shiny and beutiful?~
  IF ~~ THEN EXIT
END

IF ~

RandomNum(10,6)
~ THEN BEGIN FM5 // from:
  SAY ~Can't chat with you, I am doing my job.~
  IF ~~ THEN EXIT
END

IF ~

RandomNum(10,7)
~ THEN BEGIN FM6 // from:
  SAY ~Don't try to steal something, and I will not try to cut off something.~
  IF ~~ THEN EXIT
END

IF ~

RandomNum(10,8)
~ THEN BEGIN FM7 // from:
  SAY ~I wish I would be on vacation soon.~
  IF ~~ THEN EXIT
END

IF ~

RandomNum(10,9)
~ THEN BEGIN FM8 // from:
  SAY ~Don't touch me. I am on duty and will not go easy on you.~
  IF ~~ THEN EXIT
END

IF ~

RandomNum(10,10)
~ THEN BEGIN FM9 // from:
  SAY ~Somebody, replace me for a minute, I need to go to toilet...~
  IF ~~ THEN EXIT
END


